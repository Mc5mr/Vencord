# AFK Panel — Mc5mr

إضافة تجريبية لنسخة `6mmr/Vencord`، ومعها خدمة تشغيل مستقلة. الأوثر: **Mc5mr**، ومعرّف ديسكورد `523157034141745153`.

تضيف زر سماعة باسم **AFK Panel** في الشريط العلوي. تدخل توكن حساب تملكه أو لديك إذن باستخدامه، وآيدي السيرفر، وآيدي الروم الصوتي، ثم تضغط **Start AFK**. الخدمة تحاول إدخال الحساب مكتوم المايك والصوت. تعرض اللوحة **Connected** بعد تأكيد اتصال الصوت، وتتيح إيقاف الجلسة.

**الحالة:** نجح البناء والفحص المحلي والمحاكاة. لم تُختبر جلسة دخول حقيقية إلى Discord أو الاتصال بخادم Lavalink الخاص بك، ولا يوجد ضمان باستمرار توافق حسابات المستخدم. مكتبة `discord.js-selfbot-v13` المستعملة مؤرشفة وتوقفت صيانتها. تشغيل حساب مستخدم آليًا يخالف شروط Discord ويمكن أن يؤدي إلى تعطيل الحساب. لا يوجد تجاوز لكلمة المرور أو التحقق الثنائي أو صلاحيات الرومات.

## ما الذي تم تجهيزه؟

- `src/plugins/afkPanel.discordDesktop`: البلوقن وواجهة التحكم.
- `afkpanel-service`: خدمة Node.js تستقبل أوامر اللوحة وتتصل بـDiscord وLavalink.
- `AFKPanel.patch` في ملف التسليم: كل إضافات الكود وإصلاحا البناء، لتطبيقها على المستودع.
- `INSTALL_WINDOWS.cmd` في ملف التسليم: تجهيز نسخة منفصلة وبناؤها على ويندوز.

الخدمة مستقلة عن ملفات مشروع الموسيقى الذي أرسلته. الملف الأصلي يستخدم بوتات Developer Portal وMoonlink، ويعتمد أيضًا على ملفات غير مرفقة، منها `stability.cjs` و`handler` ومسار إعدادات خاص بالخادم. يتشارك المشروع الجديد تنسيق `host.json` فقط، وتختار عقدة صوت واحدة صراحة.

## 1. تجهيز نسخة المستودع على ويندوز

إذا كان الفرع `mc5mr-afk-panel` مرفوعًا على GitHub، يمكنك تنزيله وتشغيل البناء مباشرة من CMD:

```bat
cd /d "%USERPROFILE%"
git clone --branch mc5mr-afk-panel https://github.com/6mmr/Vencord.git Mc5mr-Vencord
cd Mc5mr-Vencord
pnpm install --frozen-lockfile
pnpm build
```

إذا حمّلت حزمة ZIP من المحادثة، استخدم الخطوات التالية:

1. فك ضغط `AFKPanel-Mc5mr.zip` كاملًا إلى مجلد.
2. شغّل `INSTALL_WINDOWS.cmd`. يحتاج Git وNode.js 22 أو أحدث وpnpm، وهي أدوات البناء التي استخدمتها سابقًا.
3. السكربت ينشئ مجلدًا منفصلًا اسمه `Mc5mr-Vencord` داخل مجلد مستخدم ويندوز، ويطبّق التغييرات ثم يشغّل البناء. يتوقف إذا كان هذا المجلد موجودًا حتى لا يكتب فوق عمل سابق.
4. بعد ظهور نجاح البناء، افتح **CMD** واكتب:

```bat
cd /d "%USERPROFILE%\Mc5mr-Vencord"
pnpm inject
```

5. اختر **Discord Canary** إذا كانت هي النسخة التي تستخدمها. اقفل Discord بالكامل من أيقونته بجانب الساعة ثم افتحه.
6. ادخل إعدادات Vencord ثم **Plugins**، وابحث عن **AFKPanel** وفعّله. نفّذ إعادة التشغيل إن طُلبت.
7. افتح زر السماعة بالأعلى. يوجد أيضًا زر **Open AFK Panel** داخل معلومات البلوقن إذا تغيّر شكل شريط Discord ولم يظهر الزر.

هذا يثبت نسخة `6mmr/Vencord` التي طلبت العمل عليها. التغييرات مبنية على الالتزام `b21a42f50be0f7e9dbc21c8209f521f2828a375b`.

لمن لديه نسخة عمل جاهزة ويريد تطبيق التغييرات يدويًا: أنشئ فرعًا جديدًا، ثم استخدم `git apply --check` و`git apply` مع المسار الكامل لملف `AFKPanel.patch`. لا تستبدل ملفات معدّلة يدويًا إذا فشل فحص التطبيق.

## 2. تشغيل خدمة AFK

تحتاج الخدمة جهازًا أو استضافة يمكنها تشغيل Node.js. ملف `host.json` هو بيانات اتصال بخادم الصوت؛ لا يشغّل برنامج AFK وحده. يمكن تشغيل الخدمة على كمبيوترك للتجربة، أو على الاستضافة لتظل تعمل عند إغلاق الكمبيوتر.

1. انسخ `host.json` عندك من `music/settings` إلى مجلد `afkpanel-service`. احتفظ بالقيم الحقيقية عندك.
2. افتح CMD داخل مجلد `afkpanel-service` وشغّل:

```bat
npm ci
npm run configure
```

3. افتح الملف الجديد `afkpanel.local.json` بالمفكرة. لا تشاركه؛ يحتوي على `apiKey` خاص بخدمتك.
4. اضبط `lavalinkNode` حسب موقع خادم الصوت:

| القيمة | معناها في ملف host.json الذي أرسلته |
| --- | --- |
| `local-primary` | العقدة الأولى. عنوان `127.0.0.1` يعني نفس الجهاز الذي يشغّل خدمة AFK. استخدمه إذا كان Lavalink يعمل على ذلك الجهاز. |
| `node-2` | العقدة الثانية الخارجية، لأن خانة `identifier` غير موجودة لها. استخدم هذه القيمة إذا كان هذا هو الخادم المتاح لك. |

5. شغّل:

```bat
npm start
```

اترك نافذة التشغيل مفتوحة. لوقف الخدمة وكل جلساتها اضغط **Ctrl + C**. الحد الافتراضي ثلاث جلسات، ويمكن ضبطه بين جلسة وخمس جلسات في `maxSessions`.

يحتاج خادم الصوت Lavalink API v4 ودعم تشفير الصوت الحالي في Discord. إذا ظهرت مشكلة اتصال صوت، تأكد من إصداره وصلاحيات الحساب وتطابق آيدي السيرفر والروم. بيانات العقدة الخارجية السابقة لم يمكن التأكد من استجابتها من بيئة الفحص.

إذا كانت `secure` تساوي `false` فالاتصال بعقدة Lavalink غير مشفّر؛ استخدم عقدة محلية أو اتصال TLS/نفقًا خاصًا عندما توفر الاستضافة ذلك. لا يصل توكن الحساب إلى Lavalink؛ يصله معرّف الحساب وبيانات جلسة الصوت اللازمة للاتصال.

## 3. ربط اللوحة بالخدمة

1. افتح **AFK Panel → Connection**.
2. إذا كانت الخدمة على نفس الكمبيوتر، اكتب عنوانها:

```text
http://127.0.0.1:3847
```

3. انسخ `apiKey` من `afkpanel.local.json` إلى خانة **Service key** عندك داخل اللوحة.
4. اضغط **Test & save connection**. يُحفظ مفتاح الخدمة بتشفير النظام محليًا بعد نجاح الاختبار، ولا يُضاف إلى إعدادات المزامنة العادية.
5. افتح **Sessions**، وأدخل توكن حسابك وآيدي السيرفر وآيدي الروم الصوتي ثم **Start AFK**.
6. انتظر حالة **Connected**. انتهاء طلب التشغيل وحده لا يعني اكتمال اتصال الصوت.
7. استخدم **Stop** للخروج. زر **Remove** يحذف بطاقة الجلسة المنتهية.

إذا كانت الخدمة على استضافة، اتركها تستمع على `127.0.0.1` خلف reverse proxy يوفر HTTPS، واستخدم عنوان HTTPS الخاص بك في اللوحة. العنوان الخارجي للخدمة مختلف عن عنوان Lavalink. لا تضع كلمة مرور Lavalink في خانة Service key. لا تقبل اللوحة HTTP بعيدًا عن localhost.

## سلوك الجلسات والبيانات

- يغلق البلوقن نافذته دون إيقاف الجلسات؛ استمرارها يعتمد على بقاء خدمة AFK واتصالاتها شغالة.
- لا يبدأ أي حساب تلقائيًا عند تشغيل الخدمة. التوكن يُدخل يدويًا لكل جلسة، ويُحذف من حقل الإدخال عند إرسال الطلب.
- الخدمة لا تكتب توكنات الحسابات إلى الملفات. تبقى في ذاكرة عميل Discord خلال الجلسة، وتُغلق الجلسة عند الإيقاف.
- بعد إعادة تشغيل الخدمة تحتاج إدخال التوكن من جديد. إعادة الاتصال بعد فقد الصوت تتم بطلب جديد من المستخدم.
- إذا خرج الحساب أو نُقل من الروم تتوقف الجلسة. لا تحاول الخدمة تجاوز نقل إداري أو صلاحيات الروم بإعادة إدخاله.
- إذا تعذّر الوصول للخدمة تعرض اللوحة **Last known** للحالات القديمة بدل عرضها كحالات مؤكدة.
- هذا الإصدار مخصص لرومات الصوت العادية، ولا يدعم Stage أو المكالمات الخاصة أو تشغيل المقاطع.
- `host.json` و`*.local.json` و`.env` و`node_modules` مستثناة من Git داخل مجلد الخدمة. تأكد من بقاء أسرارك خارج أي ملفات ترفعها يدويًا.

## رفع التغييرات إلى مستودع صاحبك

التغييرات مخصصة لفرع مستقل اسمه `mc5mr-afk-panel`، والدمج في `main` خطوة منفصلة. حزمة التسليم تتضمن ملف التغييرات كاملًا أيضًا.

بعد استعمال السكربت، سيكون الفرع المحلي `mc5mr-afk-panel` جاهزًا. افتح CMD ونفّذ:

```bat
cd /d "%USERPROFILE%\Mc5mr-Vencord"
git add AFKPANEL_AR.md afkpanel-service src/plugins/afkPanel.discordDesktop src/api/HeaderBar.tsx src/plugins/autoTranslateNightcord/index.ts
git commit -m "Add AFK Panel by Mc5mr"
git push -u origin mc5mr-afk-panel
```

سجّل دخول حسابك في نافذة GitHub إذا طلب Git ذلك. إذا طلب Git الاسم والبريد للمرة الأولى، اضبطهما حسب بيانات حساب GitHub عندك. بعد الرفع افتح المستودع واضغط **Compare & pull request** لمراجعة الإضافة قبل دمجها في `main`.

**ملاحظة الإصدار:** سير عمل النشر الموجود في نسخة صاحبك يشترط أن يكون اسم المستودع `Vendicated/Vencord` لرفع ملفات الإصدار. رفع الكود وحده لا ينشر تحديثًا تلقائيًا لنسخة صاحبك؛ تحتاج بناء محليًا أو إعداد إصدار مناسب للمستودع.

## نتائج الفحص وحدوده

- `pnpm build`: نجح في نسخة المستودع مع الإضافة.
- `npm test` داخل الخدمة: نجحت سبعة اختبارات، منها HTTP وWebSocket حقيقيان على localhost مع بيانات وهمية، وأحداث Discord محاكاة.
- نجحت ثلاثة اختبارات لواجهة Electron: تقييد مصدر الطلبات، تشفير مفتاح الخدمة، حفظ الإعدادات السابقة عند الفشل، وعدم تخزين توكن الحساب.
- نجح اختبار تفاعل مكوّن React في JSDOM بإبدال مكوّنات Discord وواجهة الخدمة بنماذج اختبار: الإعداد، إخفاء الحقول، التشغيل، حالة الاتصال، الإيقاف والحذف.
- فحص TypeScript الكامل لا يمر في هذا المستودع: لديه **74 تشخيصًا متطابقًا** مع إضافة AFKPanel وبدونها بعد إزالة مانع التحليل القديم. الإضافة لم تضف تشخيصات جديدة.
- أُصلح مانعا بناء سابقان: إزالة `them.` من أول بلوقن الترجمة، وتحويل زر إعدادات Nightcord الذي يستورد ملفًا مفقودًا إلى صفحة البلوقنات الموجودة.
- لا توجد تجربة بحساب حقيقي أو تحقق بصري داخل Discord Canary أو اختبار لتشفير Windows الفعلي هنا. النجاح داخل الحساب والخادم عندك يحتاج التجربة الفعلية، وهذه الحزمة **تجريبية**.

مراجع التنفيذ: [المستودع المستهدف](https://github.com/6mmr/Vencord)، [مكتبة حسابات المستخدم المؤرشفة](https://github.com/aiko-chan-ai/discord.js-selfbot-v13)، [Lavalink](https://github.com/lavalink-devs/Lavalink).
